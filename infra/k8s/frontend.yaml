apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: cloudservice-demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: luukmn/cloudservice-frontend:latest
          imagePullPolicy: Always
          env:
            - name: VITE_GATEWAY_URL
              value: http://gateway.cloudservice-demo.svc.cluster.local:8000
            - name: VITE_HISTORY_URL
              value: http://history.cloudservice-demo.svc.cluster.local:9000
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: cloudservice-demo
spec:
  type: NodePort
  selector:
    app: frontend
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30081
      name: http
