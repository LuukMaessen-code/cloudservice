[project]
name = "logging_service"
version = "0.1.0"
description = "Logging service for the cloudservice application."
authors = [
    { name = "Your Name", email = "your.email@example.com" }
]
readme = "README.md"
requires-python = ">=3.11"

[tool.setuptools]
packages = ["logging_service", "logging_service.app"]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
